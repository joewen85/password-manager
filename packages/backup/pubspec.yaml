name: password_manager_backup
description: Encrypted backup interfaces.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  meta: ^1.11.0


dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.0
