name: password_manager_crypto
description: AES-256 encryption utilities for the password manager.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  cryptography: ^2.7.0
  meta: ^1.11.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.0
