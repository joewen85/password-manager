# 真机验证结果模板

日期：
设备型号：
HarmonyOS 版本：
DevEco 版本：
构建产物：

## 编译结果

- [ ] Build Hap(s)/APP(s) 成功
- [ ] 安装成功
- [ ] 启动成功

## 冒烟用例结果

1. 初始化密码库：通过/失败（说明）
2. 解锁密码库：通过/失败（说明）
3. 新建账号条目：通过/失败（说明）
4. 新建服务器条目：通过/失败（说明）
5. 新建服务条目：通过/失败（说明）
6. 标签管理与搜索：通过/失败（说明）
7. 锁定后重解锁数据保留：通过/失败（说明）
8. 杀进程重启后数据保留：通过/失败（说明）
9. 错误主密码处理：通过/失败（说明）

## 字段关联回归结果

1. 创建字段时直接选择“文本/字段关联”，无需保存后改型：通过/失败（说明）
2. 首次保存前选择目标分类和目标文本字段，稳定 ID 重启后保留：通过/失败（说明）
3. 同分类关联其他文本字段可保存，直接自引用被拒绝：通过/失败（说明）
4. 候选仅包含未删除且符合分类约束的条目，选择/更换/清空可保存：通过/失败（说明）
5. 九态详情及查看目标、重选、修复配置、清除入口：通过/失败（说明）
6. 仅 `RESOLVED` 的显式详情显示配置字段值，其他状态和搜索不泄露：通过/失败（说明）
7. 旧 `entryReference` 兼容、未知字段类型只读且元数据无损：通过/失败（说明）
8. 引用 ID、目标秘密和无关字段不进入候选、摘要、搜索、日志或剪贴板：通过/失败（说明）
9. 单条/分类导出不隐式携带目标条目，copy import 只重映射条目 ID：通过/失败（说明）
10. 同步、重启、导入及旧 Flutter 迁移边界：通过/失败（说明）

## 日志与问题

- 关键日志片段：
- 问题清单：
- 复现步骤：
- 严重级别：

## 结论

- [ ] 可进入下一阶段
- [ ] 需修复后复测
